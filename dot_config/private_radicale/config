#[server]
#hosts = 0.0.0.0:5232, [::]:5232
#max_connections = 20
#max_content_length = 100000000
#timeout = 30

[auth]
type = htpasswd
htpasswd_filename = ~/.config/radicale/users
htpasswd_encryption = md5

[storage]
filesystem_folder = ~/.local/share/calendars/radicale

# http://localhost:5232
